import { Metadata, NextPage } from "next";

export const metadata: Metadata = {
    title: "HomePage | MyTrail",
};

const HomePage: NextPage = () => {
    return <div>Home Page</div>;
};

export default HomePage;
